{
  "name": "commuter",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "mocha --recursive",
    "setup-db": "node models/setup.js",
    "start-dynamo": "cd ~/amazondynamodb;java -Djava.library.path=./DynamoDBLocal_lib -jar DynamoDBLocal.jar -sharedDb",
    "reset-db": "aws dynamodb delete-table --table-name line-status --endpoint-url http://localhost:8000; node models/setup.js",
    "debug-start": "DEBUG=* node server.js",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chai": "*",
    "debug": "^2.3.2",
    "dotenv": "^2.0.0",
    "express": "^4.14.0",
    "mocha": "*",
    "morgan": "^1.7.0",
    "request": "^2.78.0"
  },
  "devDependencies": {
    "aws-sdk": "^2.7.5",
    "debug": "^2.3.2"
  }
}
